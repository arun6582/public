#!/usr/bin/env bash

msg=$1
shift
cap=$1

printf '%*s\n' "${COLUMNS:-$(tput cols)}" '' | tr ' ' -
if [[ $cap == '--no-capitalize' || $cap == '--no-cap' ]]; then
	printf '%s\n' "$msg"
else
	y=${msg^^}
	printf '%s\n' "$y"
	# figlet "$msg"
fi
printf '%*s\n' "${COLUMNS:-$(tput cols)}" '' | tr ' ' -
